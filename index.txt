let accounts = [
{id: 1, clientId:1, mount: 50000, exetiveItemId:1},
{id: 2, clientId:2, mount: 70000, exetiveItemId:2}
];

let newAccounts = accounts.filter(({ mount }}) => mount >= 2500000);

let accountsOrder = accounts.sort((a,b) => {
    if (a.mount > b.mount) return 1;
    if (a.mount < b.mount) return -1;
});

let clients = [];

let clientsMore25000000 = return accounts.map((account) => {
    return cients.filter((client) => (client.id === account.clientId) && account > 2500000 ))
});

